
## Tool-use tip (auto-added by claude-learn)
- You often run `Edit → mcp__Claude_Preview__preview_eval → mcp__Claude_Preview__preview_screenshot` in sequence. When possible, combine these into a single bash pipe or use one broader tool call.

## Key file: styles.css
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: index.html
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: script.js
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Inline summary: index.html
<!-- Add a summary here to prevent re-reads. -->

## Inline summary: script.js
<!-- Add a summary here to prevent re-reads. -->

## Response style (auto-added by claude-learn)
- Be concise. No preamble. No summary at the end of every response.
- Use short sentences and bullet lists over paragraphs.
- Show code, not narration about the code.
- If nothing to add beyond the answer, stop writing.

## Project primer (auto-generated by claude-learn)
<!-- Refreshes when project structure changes. Helps Claude resume after context overflow without re-exploration. -->

### Structure
- `CLAUDE.md` (file)
- `CNAME` (file)
- `accent-lab.html` (file)
- `accent-lab.js` (file)
- `blog` (dir)
- `generator.html` (file)
- `generator.js` (file)
- `index.html` (file)
- `script.js` (file)
- `streaming-tracker` (dir)
- `styles.css` (file)
- `tools.css` (file)

### Stack markers

### Conventions detected
- (populated from claude-learn memory over time)

## Inline summary: styles.css
<!-- Add a summary here to prevent re-reads. -->

## Key file: blog.html
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: blog/post.html
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Inline summary: blog.html
<!-- Add a summary here to prevent re-reads. -->

## Tool-use tip (auto-added by claude-learn)
- You often run `mcp__Claude_Preview__preview_inspect → mcp__Claude_Preview__preview_inspect → mcp__Claude_Preview__preview_inspect` in sequence. When possible, combine these into a single bash pipe or use one broader tool call.

## Tool-use tip (auto-added by claude-learn)
- You often run `mcp__Claude_Preview__preview_eval → mcp__Claude_Preview__preview_screenshot → mcp__Claude_Preview__preview_resize` in sequence. When possible, combine these into a single bash pipe or use one broader tool call.

## Inline summary: blog/post.html
<!-- Add a summary here to prevent re-reads. -->

## Tool-use tip (auto-added by claude-learn)
- You often run `Bash → Edit → mcp__Claude_Preview__preview_eval` in sequence. When possible, combine these into a single bash pipe or use one broader tool call.

## Tool-use tip (auto-added by claude-learn)
- You often run `mcp__Claude_Preview__preview_screenshot → Bash → Edit` in sequence. When possible, combine these into a single bash pipe or use one broader tool call.

## Frequent search: `skillicons`
<!-- Describe where this symbol/text lives so Claude doesn't have to search. -->
